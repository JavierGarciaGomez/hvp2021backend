export enum UserRole {
  ADMIN = "admin",
  USER = "user",
  COLLABORATOR = "collaborator",
  MANAGER = "manager",
  SUPERVISOR = "supervisor",
  SELLER = "seller",
  CUSTOMER = "customer",
  GUEST = "guest",
}
